---
interface Props{
	id: string
	title?: string
}
const {id, title} = Astro.props;
---

<article id={id} class="w-full h-fit flex flex-col gap-5 p-5 text-sm">
    <h3 class="text-2xl font-mono font-bold uppercase">{title || "No title"}</h3>
	<slot/>
</article>
